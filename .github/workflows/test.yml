name: Test this action

on: 
  push:  
  pull_request:

jobs:
  test:
    name: Test action
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - run: |
          sh test.sh
